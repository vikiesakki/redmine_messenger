<% if User.current.allowed_to?(:suppress_messenger_notification, @project) %>
	<%= form.check_box :suppress_notication, :no_label => true %> <label for="issue_suppress_notication">Suppress messenger notification</label>
<% end %>